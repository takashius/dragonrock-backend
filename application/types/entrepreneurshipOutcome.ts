/** Resultado estándar de casos de entrevistas Emprende (compatible con respuestas HTTP actuales). */
export type EntrepreneurshipOutcome = {
  status: number;
  message: unknown;
  detail?: unknown;
};
